- clean: ['~']

- link:
    ~/.profile: profile
    ~/.bash_profile: bash_profile
    ~/.bashrc: bashrc
    ~/.gitconfig: gitconfig
    ~/.vimrc.local: vimrc.local
    ~/.tmux.conf: tmux.conf
    ~/.zshrc: zshrc
	
- shel:
  - ln -s ~/.vim ~/.config/nvim
  - ln -s ~/.vimrc ~/.config/nvim/init.vim
